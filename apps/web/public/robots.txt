# robots.txt for ImmoTopia Showcase Site

User-agent: *
Allow: /

# Disallow admin and API routes if they exist
Disallow: /api/
Disallow: /admin/

# Sitemap location (dynamique via Next.js)
Sitemap: ${process.env.NEXT_PUBLIC_APP_URL || 'https://immotopia.com'}/sitemap.xml

