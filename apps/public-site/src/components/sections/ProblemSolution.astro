---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;

interface ProblemCard {
  title: string;
  description: string;
}

interface SolutionCard {
  title: string;
  description: string;
}

const problems: ProblemCard[] = [
  {
    title: '⏱️ Trop de temps perdu',
    description: 'Tâches répétitives, paperasse et saisies manuelles qui ralentissent votre activité.',
  },
  {
    title: '📂 Outils dispersés',
    description: 'Excel, WhatsApp, emails, documents papier… vos informations sont éparpillées.',
  },
  {
    title: '🎯 Prospects et copropriétaires mal suivis',
    description: 'Relances oubliées, opportunités manquées, conversions difficiles.',
  },
];

const solutions: SolutionCard[] = [
  {
    title: '🚀 Plus de temps pour votre cœur de métier',
    description: 'Les processus sont automatisés pour vous concentrer sur vos clients et copropriétaires.',
  },
  {
    title: '🧩 Une seule plateforme',
    description: 'Biens, clients, locations, copropriétés et annonces centralisés au même endroit.',
  },
  {
    title: '🤝 Un CRM qui travaille pour vous',
    description: 'Suivi intelligent des clients et copropriétaires, relances automatiques et meilleures conversions.',
  },
];
---

<section class:list={['relative overflow-hidden py-16 md:py-24 lg:py-32', className]}>
  <!-- Background -->
  <div class="pointer-events-none absolute inset-0">
    <div class="absolute inset-0 bg-gradient-to-b from-white via-gray-50/30 to-white" />
    <div class="absolute top-1/4 right-0 h-[600px] w-[600px] rounded-full bg-[#2563EB]/3 blur-3xl" />
    <div class="absolute bottom-1/4 left-0 h-[500px] w-[500px] rounded-full bg-blue-50/50 blur-3xl" />
  </div>

  <div class="container relative z-10 mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <!-- Header -->
    <div class="mb-16 md:mb-20 text-center">
      <h2 class="mb-4 text-3xl font-bold leading-tight text-gray-900 sm:text-4xl md:text-5xl">
        Assez des complications. Passez à une gestion immobilière fluide.
      </h2>
      <p class="mx-auto max-w-3xl text-base text-gray-600 sm:text-lg md:text-xl">
        Les difficultés que rencontrent toutes les agences immobilières et syndics — et comment{' '}
        <span class="font-semibold text-[#2563EB]">ImmoTopia</span> les élimine.
      </p>
    </div>

    <!-- Ligne 1 - Problèmes -->
    <div class="mb-12 md:mb-16">
      <h3 class="mb-8 text-center text-lg font-semibold text-gray-700 md:text-xl">
        Ce qui vous freine aujourd'hui
      </h3>
      <div class="grid grid-cols-1 gap-6 md:gap-8 lg:grid-cols-3">
        {problems.map((problem) => (
          <div class="group relative">
            <div class="relative rounded-xl border border-gray-200/50 bg-gray-50/40 p-6 backdrop-blur-sm transition-all duration-300 hover:opacity-75 hover:border-gray-300/50">
              <div class="mb-4 flex items-center gap-3">
                <div class="flex h-12 w-12 items-center justify-center rounded-lg bg-red-50/70 text-red-500/70 transition-transform duration-300">
                  <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <circle cx="12" cy="12" r="10" stroke-width="2"/>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01"/>
                  </svg>
                </div>
                <h4 class="text-lg font-semibold text-gray-700">{problem.title}</h4>
              </div>
              <p class="text-sm leading-relaxed text-gray-600/90">{problem.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>

    <!-- Ligne 2 - Résultats -->
    <div>
      <h3 class="mb-8 text-center text-lg font-semibold text-gray-700 md:text-xl">
        Ce que vous gagnez avec ImmoTopia
      </h3>
      <div class="grid grid-cols-1 gap-6 md:gap-8 lg:grid-cols-3">
        {solutions.map((solution) => (
          <div class="group relative">
            <div class="relative overflow-hidden rounded-xl border border-[#2563EB]/20 bg-white p-6 shadow-lg transition-all duration-300 hover:-translate-y-1.5 hover:border-[#2563EB]/40 hover:shadow-2xl hover:shadow-[#2563EB]/10">
              <!-- Subtle gradient overlay on hover -->
              <div class="absolute inset-0 bg-gradient-to-br from-[#2563EB]/0 via-[#2563EB]/0 to-[#2563EB]/0 transition-all duration-300 group-hover:from-[#2563EB]/2 group-hover:via-[#2563EB]/1 group-hover:to-[#2563EB]/2" />
              <div class="relative z-10">
                <div class="mb-4 flex items-center gap-3">
                  <div class="flex h-12 w-12 items-center justify-center rounded-lg bg-gradient-to-br from-[#2563EB] via-[#1D4ED8] to-[#1E40AF] text-white shadow-lg shadow-[#2563EB]/25 transition-all duration-300 group-hover:scale-110 group-hover:shadow-xl group-hover:shadow-[#2563EB]/40">
                    <svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                    </svg>
                  </div>
                  <h4 class="text-lg font-semibold text-gray-900">{solution.title}</h4>
                </div>
                <p class="text-sm leading-relaxed text-gray-700">{solution.description}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
