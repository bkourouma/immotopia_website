---
// Hero section for Astro - simplified version
---

<!-- Sentinel pour IntersectionObserver - placé juste avant le hero -->
<div id="hero-sentinel" class="absolute top-0 left-0 w-full h-1 pointer-events-none" aria-hidden="true"></div>

<section class="relative overflow-hidden min-h-[calc(100vh-44px)] flex items-center justify-center bg-gradient-to-br from-[#F0F7FF] via-[#E8F2FF] to-[#D6EBFF] mt-[44px]">
  <!-- Video Background (optional) -->
  <div class="absolute inset-0 w-full h-full z-0">
    <video
      id="hero-video"
      autoplay
      loop
      muted
      playsinline
      class="absolute inset-0 w-full h-full object-cover"
      aria-label="Démonstration du logiciel de gestion immobilière ImmoTopia"
      style="display: none;"
    >
      <source src="/videos/logiciel_gestion_immobilière.mp4" type="video/mp4" />
    </video>
    <!-- Dark overlay -->
    <div id="video-overlay" class="absolute inset-0 bg-gradient-to-b from-black/60 via-black/40 to-black/60" />
  </div>

  <!-- Animated Gradient Background Overlay -->
  <div class="pointer-events-none absolute inset-0 overflow-hidden z-[1]">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_20%_50%,rgba(37,99,235,0.12),transparent_50%)]" />
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_80%_80%,rgba(29,78,216,0.12),transparent_50%)]" />
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_20%,rgba(59,130,246,0.10),transparent_50%)]" />
    
    <!-- Animated floating orbs -->
    <div class="absolute top-20 left-10 h-72 w-72 rounded-full bg-[#2563EB]/15 blur-3xl animate-pulse" />
    <div class="absolute bottom-20 right-10 h-96 w-96 rounded-full bg-[#1D4ED8]/15 blur-3xl animate-pulse" style="animation-delay: 1s;" />
    
    <!-- Grid pattern overlay -->
    <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px]" />
  </div>

  <div class="container relative z-10 mx-auto max-w-[1200px] px-4 sm:px-6 pt-24 pb-12 md:pt-28 md:pb-16 lg:pt-32 lg:pb-20">
    <div class="flex justify-center">
      <div class="w-full text-center lg:w-2/3 xl:w-[58%]">
        <!-- Badge -->
        <div class="mb-6 md:mb-8 inline-flex items-center gap-2 rounded-full border border-[#2563EB]/30 bg-white/80 backdrop-blur-md px-4 py-2 md:px-5 md:py-2.5 shadow-lg transition-all duration-300 hover:scale-105">
          <svg class="h-4 w-4 text-[#2563EB] animate-pulse" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
          </svg>
          <span class="text-xs font-bold uppercase tracking-wider text-[#2563EB]">
            Logiciel immobilier cloud
          </span>
        </div>

        <!-- Title -->
        <h1 class="mb-6 md:mb-8 flex flex-col items-center text-2xl font-extrabold leading-tight text-white drop-shadow-lg sm:text-3xl md:text-4xl lg:text-5xl">
          <span class="block">
            Gérez votre agence immobilière{' '}
            <span class="relative inline-block">
              <span class="absolute inset-0 bg-gradient-to-r from-[#2563EB] via-[#3B82F6] to-[#2563EB] bg-[length:200%_100%] bg-clip-text opacity-75 blur-sm" />
              <span class="relative bg-gradient-to-r from-[#2563EB] via-[#1D4ED8] to-[#2563EB] bg-[length:200%_100%] bg-clip-text text-transparent">
                en un seul outil
              </span>
            </span>
          </span>
          <span class="block text-white drop-shadow-lg">plus vite, plus simple</span>
        </h1>

        <!-- Description -->
        <p class="mx-auto mb-6 md:mb-8 max-w-[750px] text-sm leading-relaxed text-white/95 drop-shadow-md sm:text-base md:text-lg lg:text-xl font-medium px-4 sm:px-0">
          Centralisez vos biens, vos contacts, vos locations et la publication de vos annonces sur une plateforme unique, pensée pour les professionnels de l'immobilier en Afrique de l'Ouest.
        </p>

        <!-- Benefit bullets -->
        <div class="mx-auto mb-8 md:mb-10 flex flex-wrap justify-center gap-4 sm:gap-6 px-4 sm:px-0">
          <div class="flex items-center gap-2 text-sm sm:text-base text-white/95 drop-shadow-md">
            <div class="h-2 w-2 rounded-full bg-white"></div>
            <span>Suivi complet des biens et des dossiers</span>
          </div>
          <div class="flex items-center gap-2 text-sm sm:text-base text-white/95 drop-shadow-md">
            <div class="h-2 w-2 rounded-full bg-white"></div>
            <span>CRM + rendez-vous + relances</span>
          </div>
          <div class="flex items-center gap-2 text-sm sm:text-base text-white/95 drop-shadow-md">
            <div class="h-2 w-2 rounded-full bg-white"></div>
            <span>Gestion locative et paiements simplifiés</span>
          </div>
        </div>

        <!-- CTA Group -->
        <div class="mb-8 md:mb-12 flex flex-col items-center justify-center gap-4 sm:gap-5 sm:flex-row px-4 sm:px-0">
          <a
            href="/contact?signup=true"
            class="group relative w-full overflow-hidden bg-[#2563EB] text-white shadow-xl transition-all duration-300 hover:scale-105 hover:shadow-2xl hover:bg-[#1D4ED8] sm:w-auto px-6 py-5 md:px-8 md:py-6 text-sm sm:text-base font-bold rounded-lg flex items-center justify-center"
            id="cta-hero-signup"
          >
            Créer mon compte
            <svg class="ml-2 h-4 w-4 sm:h-5 sm:w-5 transition-transform duration-300 group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
            </svg>
          </a>
          <a
            href="/contact?demo=true"
            class="group w-full border-2 border-gray-300 bg-white/90 backdrop-blur-md transition-all duration-300 hover:scale-105 hover:border-[#2563EB] hover:bg-white hover:shadow-xl sm:w-auto px-6 py-5 md:px-8 md:py-6 text-sm sm:text-base font-semibold text-gray-800 rounded-lg flex items-center justify-center"
          >
            Voir la démo
          </a>
        </div>
        
        <!-- Micro-copy -->
        <p class="text-center text-xs sm:text-sm text-white/90 drop-shadow-md px-4">
          Sans engagement · Configuration rapide · Support inclus
        </p>
      </div>
    </div>
  </div>
</section>

<script>
  // Gestion optionnelle de la vidéo avec fallback
  const video = document.getElementById('hero-video');
  const overlay = document.getElementById('video-overlay');
  
  if (video) {
    // Afficher la vidéo par défaut
    video.style.display = 'block';
    
    // Si la vidéo ne charge pas, utiliser un fond dégradé plus clair
    video.addEventListener('error', () => {
      video.style.display = 'none';
      if (overlay) {
        overlay.className = 'absolute inset-0 bg-gradient-to-b from-black/20 via-black/10 to-black/20';
      }
    });
    
    // Forcer le chargement
    video.load();
  }
</script>

